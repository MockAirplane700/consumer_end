import 'package:flutter/material.dart';
// the color of the application app bar
const Color primaryAppBarColor = Colors.blueAccent;

// the color of the background in the application
const Color primaryBackgroundColor = Colors.white;

// the color of any cards made
const Color primaryCardBackgroundColor = Colors.white;

// the color of text
const Color primaryTextColor = Colors.black;